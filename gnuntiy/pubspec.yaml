name: gnunity
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.27.0
  cloud_firestore: ^4.17.3
  crypto: ^3.0.3
  # 아이콘 사용을 위한 패키지
  cupertino_icons: ^1.0.2
  table_calendar: ^3.1.0
  intl: ^0.19.0
  share_plus: ^9.0.0
  #audioplayers: ^6.0.0

  # 상태 관리를 위한 패키지 (테마 변경 등)
  provider: ^6.0.5

  # Firebase 연동을 위한 필수 패키지 (추후 연동 시 필요)
  # firebase_core: ^2.15.1
  # firebase_auth: ^4.7.3
  # cloud_firestore: ^4.8.4
  # firebase_messaging: ^14.6.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - images/logo.png